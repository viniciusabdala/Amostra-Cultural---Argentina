import 'package:flutter/material.dart';

class Futebol extends StatelessWidget{
  const Futebol({super.key});

  @override
  Widget build(BuildContext context){
    return Scaffold(
      body: Center(
        child: Text("FUTEBOLLLL"),
      ),
    );
  }
}